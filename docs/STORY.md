# AURA MCP for agents

The MCP server at https://github.com/aurafhe-official/mcp provides a diagnostic workflow for agents to orchestrate arithmetic on encrypted handles. Owners prepare and decrypt data in a separate application. The wrapper boundary is implemented; the supplied native engine has not passed the confidentiality release gate. Synthetic demonstrations can validate integration, but production privacy claims require an engine fix and independent review.
